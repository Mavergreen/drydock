build-native/
build-cross/
# ctest scratch. Not build/ wholesale: build/msc.sh is tracked, and this repo's
# rule is to stage explicit paths, so an untracked log sitting next to it is
# exactly what a careless `git add build/` would take.
build/Testing/
/dist/
/VERSION
CMakeUserPresets.json
